/**
 * Domain modules (auth, authority, application, workflow, approval, audit, config,
 * idempotency, policy, integration, aml, document) will be registered here.
 */

export { HealthModule } from './health/health.module';
